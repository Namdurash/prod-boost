import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'relative',
  },
  canvas: {
    position: 'absolute',
  },
  textInput: {
    position: 'absolute',
    padding: 0,
  },
});
