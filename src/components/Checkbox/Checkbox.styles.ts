import { StyleSheet } from 'react-native';

import { COLORS } from '../../constants/Colors';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: COLORS.softAquamarine,
    borderRadius: 4,
  },
  filledBackground: {
    backgroundColor: COLORS.softAquamarine,
  },
});
